<% get_customized_menu_user.menu_entries.order('menu_entries.order ASC').each do |entry| %>
    <li>
      <%= link_to entry.title, article_path(entry.article_id) if
      entry.link_type=="article" %>
    </li>
<% end %>
