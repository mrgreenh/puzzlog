//This code is a workaround and a temporary patch for remotipart's problem with evaluating the script returned by the server
</textarea>
<script>
	<% @displayed_message = flash[:success] || flash[:errors] %>

	var old_class = parent.document.querySelector("#genericModalWindow").className;
	parent.document.querySelector("#genericModalWindow").className = old_class.replace("in","out");
	alert("<%= @displayed_message  %>");
</script>
<textarea>