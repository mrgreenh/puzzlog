<% if is_blog_initialized? %> <% #menù vuoto se blog non inizializzato 
	%>
<header class="navbar navbar-fixed-top">
	<nav class="navbar-inner">
        <div class="container">
        	<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
		      <span class="icon-bar"></span>
		      <span class="icon-bar"></span>
		      <span class="icon-bar"></span>
		    </a>
        	<%= link_to 'PuzzLog', root_path, class: 'brand' %>
        	<div class="nav-collapse">
				<ul class="nav">
					<li>
						<%= link_to 'Staff', users_path %>
					</li>
					<li class="divider-vertical"></li>
				  <% if not signed_in? %>
				  <li><a data-toggle="modal" href="#modalLogin" ><i class="icon-user"></i></a></li>
				  
				<% else signed_in? %>
					<% if can_create_fragments? %> <!-- allora mostro creazione frammenti e puzzle box -->
						<li><a href="/box"><i class="icon icon-folder-open"></i>Box</a></li>
						<li class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown">
								<b class="caret"></b>
								<i class="icon icon-comment"></i> New Fragment
							</a>
							<ul class="dropdown-menu"> 
								<% #FragmentType.most_used.limit(5).each do |type|
									FragmentType.all.each do |type|
								%>
								<li>
									<%= link_to new_fragment_path(fragment_type_id:type.id) do %>
										<img src="<%= getFragmentTypeIconThumb(type) %>" title="<%= type.name %>"> <%= type.name %>
										<li class="divider"></li>
									<% end %>
								</li>
								<% end %>
								<li><%= link_to 'More...', fragment_types_path(), remote:true %></li>
								<% if can_create_fragment_types? %>
								<li><%= link_to 'New type', new_fragment_type_path %></li>
								<% end %>
							</ul>
						</li>
					<% end %>
					<li class="dropdown">
					    <a href="#"
					          class="dropdown-toggle"
					          data-toggle="dropdown">
					          <b class="caret"></b>
					          <i class="icon-cog"></i> Manage
					    </a>
					    <ul class="dropdown-menu">
					      <% if can_create_articles?
					      %>
						      <li class="nav-header">
						      	Articles
						      </li>
						      <li>
						      	<%= link_to 'Create', new_article_path, remote: true %>
						      </li>
						      <li>
						      	<%= link_to 'Manage', articles_path %>
						      </li>
						      <li class="divider"></li>
					      <% end %>
					      <% if can_create_users? %>
						      <li class="nav-header">
						      	Users
						      </li>
						      <li>
						      	<%= link_to 'Create', new_user_path %>
						      </li>
						      <li>
						      	<%= link_to 'Manage', users_path %>
						      </li>
						      <li class="divider"></class>
					      <% end %>
					      <% if can_modify_blog? %>
								<li>
						      		<a href="#">Blog Preferences</a>
						      	</li>
						  <% end %>
					      <li>
					      	<%= link_to 'Profile', user_path(current_user) %>
					      </li>
					      <li>
					      	<%= link_to 'Signout', signout_path, method: "delete" %>
					      </li>
					    </ul>
					 </li>
				<% end %>
				</ul>
			</div>
        </div>
      </nav>
</header>
<% end %>




