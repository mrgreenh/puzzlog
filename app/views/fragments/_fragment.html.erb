<% # TODO da modificare perchè sia un sommario, con popover sempre attivi
fragment ||= @fragment
edit ||= false
popover_controls ||= false
@popover_controls ||= popover_controls
 %>
 <%= link_to fragment.name, fragment_path(fragment) %>
<% if not edit %>

	<%= render 'fragments/fragment_view', fragment: fragment %>

<% else %>
	
	<%= render 'fragments/fragment_edit', fragment: fragment %>

<% end %>
