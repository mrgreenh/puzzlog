<%= form_tag user_box_fragment_relationships_path(), method: :post, remote: :true do %>
	<%= hidden_field_tag "id", @fragment.id %>
	<%= render 'box/add_to_box_modal_form' %>
	<%= submit_tag 'Add', class:"btn btn-success", id:"add_box_fragment_confirm_button" %>
<% end %>
