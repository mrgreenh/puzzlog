//This is the original code, to place back when remotipart will work again

modalWindow("","<%= escape_javascript(render 'layouts/flash_messages') %>");

//Update the object that might have requested the upload
fragmentImagesInjection(<%= "#{@created_images.to_json}".html_safe %>);

//Update anything in the page regarding bags
<%= render 'bags/update_views' %>
